import "../views/layout.melt";

class ResourceController {
    method index() {
        let view = View();
        view.renderResource();
        setResponseBody(view.output);
        setResponseStatus(200);
        return;
    }
}
