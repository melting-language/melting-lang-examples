// Database config for MySQL (required for blog: make with-mysql).
// Use: import "config/database.melt" then mysqlConnect(db.host, db.user, db.password, db.database)

class DatabaseConfig {
    method init() {
        this.host = "localhost";
        this.user = "sanjaya";
        this.password = "redhacker";
        this.database = "melt_site";
    }
}

let db = DatabaseConfig();
