class Calc {
    method init() {}
    method add(a, b) {
        return a + b;
    }
    method greet(name) {
        return "Hello, " + name;
    }
}

let c = Calc();
let sum = c.add(3, 5);
print sum;

let msg = c.greet("Melt");
print msg;
