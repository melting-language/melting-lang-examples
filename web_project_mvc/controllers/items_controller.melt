import "../views/layout.melt";
import "../models/items.melt";

class ItemsController {
    method index() {
        let view = View();
        view.renderItems(itemsList);
        setResponseBody(view.output);
        setResponseStatus(200);
        return;
    }
}
